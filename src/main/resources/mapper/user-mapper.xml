<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace ="com.tri.evre.user.model.dao.UserMapper">
	
	<!-- 아이디 중복 체크 -->
	<select id="validateDuplicateUserId">
		SELECT
				COUNT(*)
		  FROM
				SEMI_USER
		 WHERE
				USER_ID = #{userId}
	</select>
	
	<insert id="singup">
		INSERT
		  INTO
				SEMI_USER
				(
				USER_ID
			 ,	USER_PWD
			 ,	USER_NAME
			 ,	EMAIL
				)
		VALUES
				(
				#{userId}
			 ,	#{userPwd}
			 ,	#{userName}
			 ,	#{email}
				)
	</insert>
	
	<select id="findPwd">
		SELECT
				USER_PWD
		  FROM
		  		SEMI_USER
		 WHERE
		 		USER_ID = #{userId}
	</select>
	
	<update id="update">
		UPDATE
				SEMI_USER
		   SET
		   		USER_PWD = #{userPwd}
		   	 ,	EMAIL = #{email}
		 WHERE
		 		USER_ID = #{userId}
		   AND
		   		STATUS = 'Y'
	</update>
</mapper>